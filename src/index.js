import React from 'react';
import ReactDOM from 'react-dom';
import './styles.scss';
import { PotterApp } from './PotterApp';

ReactDOM.render(
  <React.StrictMode>
    <PotterApp />
  </React.StrictMode>,
  document.getElementById('root')
);
